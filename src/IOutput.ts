import { ITransform } from './ITransform';

/**
 * output transform.
 *
 * @export
 * @interface IOutput
 * @extends {ITransform}
 */
export interface IOutput extends ITransform {
    // dts?: ITransform;
    // js?: ITransform
}
